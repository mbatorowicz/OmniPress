---
interface Props {
	dense?: boolean;
	wrap?: boolean;
	class?: string;
	wrapClass?: string;
	id?: string;
}

const { dense, wrap = true, class: className = '', wrapClass = '', id } = Astro.props;
const tableClass = dense ? 'ui-table-dense' : 'ui-table';
---

{
	wrap ? (
		<div class:list={['ui-table-wrap', wrapClass]}>
			<table class:list={[tableClass, className]} {id}>
				<slot />
			</table>
		</div>
	) : (
		<table class:list={[tableClass, className]} {id}>
			<slot />
		</table>
	)
}
