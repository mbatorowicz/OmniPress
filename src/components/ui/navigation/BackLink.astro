---
interface Props {
	href: string;
	label: string;
	class?: string;
}

const { href, label, class: className = '' } = Astro.props;
---

<p class={`mb-4 ${className}`}>
	<a href={href} class="ui-link">← {label}</a>
</p>
