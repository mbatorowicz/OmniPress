---
export type PanelVariant = 'default' | 'accent' | 'info' | 'danger' | 'amber' | 'sky';

interface Props {
	variant?: PanelVariant;
	class?: string;
	tag?: 'section' | 'div' | 'article' | 'form' | 'ul';
}

const { variant = 'default', class: className = '', tag: Tag = 'section' } = Astro.props;
---

<Tag class={`ui-panel ui-panel--${variant} ${className}`}>
	<slot />
</Tag>
