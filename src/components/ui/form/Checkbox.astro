---
interface Props {
	id?: string;
	name?: string;
	form?: string;
	value?: string;
	checked?: boolean;
	disabled?: boolean;
	class?: string;
}

const { id, name, form, value, checked, disabled, class: className = '' } = Astro.props;
---

<input
	type="checkbox"
	{id}
	{name}
	{form}
	{value}
	checked={checked}
	{disabled}
	class:list={['ui-checkbox', className]}
/>
