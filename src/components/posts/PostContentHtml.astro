---
type Props = {
	html: string;
	class?: string;
};

const { html, class: className = 'post-content-html' } = Astro.props;
---

<div class:list={[className]} set:html={html} />

<script>
	import { initPdfViewers } from '@/lib/pdf-viewer/init';
	initPdfViewers();
</script>
