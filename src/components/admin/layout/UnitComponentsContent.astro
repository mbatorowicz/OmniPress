---
import UnitComponentsRegistryForm from '@/components/admin/layout/UnitComponentsRegistryForm.astro';
import type { LayoutEditorContext } from '@/lib/admin/layout-editor-context';

interface Props {
	siteId: string;
	ctx: LayoutEditorContext;
}

const { siteId, ctx } = Astro.props;
---

<UnitComponentsRegistryForm {siteId} {ctx} />
