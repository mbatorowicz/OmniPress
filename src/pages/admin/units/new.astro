---
import AppLayout from '@/layouts/AppLayout.astro';
import { admin, adminDestinations, adminSites, adminUnit } from '@/i18n';

const errorCode = Astro.url.searchParams.get('error');
const unitErrors = adminUnit.errors;
const errorMessage =
	errorCode && errorCode in unitErrors ? unitErrors[errorCode as keyof typeof unitErrors] : null;
---

<AppLayout title={adminUnit.title} heading={adminUnit.title}>
	<a href="/admin/sites" class="mb-4 inline-block text-sm text-brand hover:underline">
		← {adminSites.title}
	</a>

	<p class="mb-4 text-sm text-slate-600">{adminUnit.lead}</p>
	<p class="mb-4 rounded-lg bg-slate-50 px-3 py-2 text-sm text-slate-700">{adminUnit.credentialsNote}</p>
	{errorMessage && (
		<p class="mb-4 rounded-lg bg-red-50 px-3 py-2 text-sm text-red-800">{errorMessage}</p>
	)}

	<form method="post" action="/api/admin/units/create" class="max-w-2xl space-y-6">
		<section class="space-y-4 rounded-xl border bg-white p-6">
			<h2 class="text-sm font-semibold text-slate-800">{adminUnit.sections.unit}</h2>
			<label class="block text-sm">
				<span class="font-medium">{adminSites.fields.name}</span>
				<input name="name" required placeholder="UG Miedzna" class="mt-1 w-full rounded-lg border px-3 py-2" />
			</label>
			<label class="block text-sm">
				<span class="font-medium">{adminSites.fields.slug}</span>
				<input name="slug" placeholder="ug-miedzna" class="mt-1 w-full rounded-lg border px-3 py-2 font-mono" />
			</label>
			<label class="flex items-center gap-2 text-sm">
				<input type="checkbox" name="is_active" checked />
				{adminSites.fields.active}
			</label>
		</section>

		<section class="space-y-4 rounded-xl border bg-white p-6">
			<label class="flex items-center gap-2 text-sm font-medium">
				<input type="checkbox" name="enable_wordpress" id="enable-wp" checked />
				{adminUnit.fields.enableWordpress}
			</label>
			<div id="panel-wp" class="space-y-3 border-l-2 border-slate-100 pl-4">
				<label class="block text-sm">
					<span>{adminDestinations.fields.wpRestBase}</span>
					<input
						name="wp_rest_base"
						id="input-wp-rest"
						placeholder="https://gmina-miedzna.pl/wp-json/wp/v2"
						class="mt-1 w-full rounded-lg border px-3 py-2"
					/>
				</label>
				<label class="block text-sm">
					<span>{adminDestinations.fields.wpUsername}</span>
					<input name="wp_username" autocomplete="off" class="mt-1 w-full rounded-lg border px-3 py-2" />
				</label>
				<label class="block text-sm">
					<span>{adminDestinations.fields.wpAppPassword}</span>
					<input name="wp_app_password" type="password" autocomplete="new-password" class="mt-1 w-full rounded-lg border px-3 py-2" />
				</label>
			</div>
		</section>

		<section class="space-y-4 rounded-xl border bg-white p-6">
			<label class="flex items-center gap-2 text-sm font-medium">
				<input type="checkbox" name="enable_astro" id="enable-astro" checked />
				{adminUnit.fields.enableAstro}
			</label>
			<div id="panel-astro" class="space-y-3 border-l-2 border-slate-100 pl-4">
				<label class="block text-sm">
					<span>{adminDestinations.fields.repo}</span>
					<input name="repo" id="input-repo" placeholder="owner/gmina-miedzna.pl" class="mt-1 w-full rounded-lg border px-3 py-2" />
				</label>
				<label class="block text-sm">
					<span>{adminDestinations.fields.branch}</span>
					<input name="branch" value="main" class="mt-1 w-full rounded-lg border px-3 py-2" />
				</label>
				<label class="block text-sm">
					<span>{adminDestinations.fields.contentPath}</span>
					<input name="content_path" value="src/content/aktualnosci" class="mt-1 w-full rounded-lg border px-3 py-2" />
				</label>
				<label class="block text-sm">
					<span>{adminDestinations.fields.githubToken}</span>
					<input name="github_token" type="password" autocomplete="new-password" class="mt-1 w-full rounded-lg border px-3 py-2" />
				</label>
			</div>
		</section>

		<section class="rounded-xl border bg-white p-6">
			<p class="text-sm font-semibold text-slate-800">{adminUnit.sections.defaultChannel}</p>
			<div class="mt-3 flex flex-wrap gap-4 text-sm">
				<label class="flex items-center gap-2">
					<input type="radio" name="default_channel" value="wordpress" checked />
					{adminDestinations.types.wordpress}
				</label>
				<label class="flex items-center gap-2">
					<input type="radio" name="default_channel" value="github_astro" />
					{adminDestinations.types.github_astro}
				</label>
			</div>
		</section>

		<button type="submit" class="rounded-lg bg-brand px-5 py-2.5 text-sm font-medium text-white">
			{adminUnit.actions.create}
		</button>
	</form>

	<p class="mt-4 text-sm text-slate-500">
		<a href="/admin/sites/new" class="text-brand hover:underline">{adminSites.newSite}</a>
		{' '}— tylko strona bez destynacji
	</p>
</AppLayout>

<script>
	const enableWp = document.getElementById('enable-wp') as HTMLInputElement | null;
	const enableAstro = document.getElementById('enable-astro') as HTMLInputElement | null;
	const panelWp = document.getElementById('panel-wp');
	const panelAstro = document.getElementById('panel-astro');
	const wpRest = document.getElementById('input-wp-rest') as HTMLInputElement | null;
	const repo = document.getElementById('input-repo') as HTMLInputElement | null;

	function sync() {
		const wpOn = enableWp?.checked ?? false;
		const astroOn = enableAstro?.checked ?? false;
		panelWp?.classList.toggle('opacity-50', !wpOn);
		panelAstro?.classList.toggle('opacity-50', !astroOn);
		if (wpRest) wpRest.required = wpOn;
		if (repo) repo.required = astroOn;
	}
	enableWp?.addEventListener('change', sync);
	enableAstro?.addEventListener('change', sync);
	sync();
</script>
