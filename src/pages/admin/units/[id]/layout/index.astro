---
import { DEFAULT_LAYOUT_TAB, layoutTabHref } from '@/lib/admin/layout-editor-tabs';

const { id } = Astro.params;
if (!id) return Astro.redirect('/admin/sites');

return Astro.redirect(layoutTabHref(id, DEFAULT_LAYOUT_TAB), 301);
---
